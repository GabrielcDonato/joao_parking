import 'package:flutter/material.dart';

class NaviBarWidget extends StatelessWidget {
  const NaviBarWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
